<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=h, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900">
      <div class="form text-white">
        <form class="p-10" action="/create" method="POST">
          <input
            class="bg-zinc-800 block px-5 rounded-lg w-full outline-none"
            type="text"
            name="title"
            placeholder="tittle goes here"
          />
          <textarea
            class="bg-zinc-800 mt-2 block w-full rounded-lg px-5 outline-none"
            placeholder="write you task details"
            name="details"
            id=""
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-full"
            type="submit"
          />
        </form>
      </div>

      <div class="tasks p-10 flex flex-wrap gap-3">
        <div
          style="
            color: yellow;
            background: #222;
            padding: 5px;
            margin-bottom: 10px;
          "
        >
          files: <%= JSON.stringify(files) %>
        </div>
        <% if (files && files.length > 0) { %> <% files.forEach(function(val) {
        %>
        <div class="rounded-md w-72 px-3 py-4 bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
          <a class="text-blue-500 inline-block mt-2" href="/files/<%= val %>"
            >Read More</a
          >
        </div>
        <% }); %> <% } else { %>
        <div class="rounded-md w-72 px-3 py-4 bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter">No tasks found</h1>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
